{
  "name": "@termweb/mux-client",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:legacy": "bun build src/index.ts --outfile=client.js --minify --format=iife",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "@types/dom-webcodecs": "^0.1.11",
    "@webgpu/types": "^0.1.69",
    "svelte": "^5.49.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "jmuxer": "^2.1.0"
  }
}
